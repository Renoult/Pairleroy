*** Begin Patch
*** Update File: app.js
@@
-  for (let i = 0; i < colorCount; i++) {
-    const cell = document.createElement('span');
-    cell.className = 'stats-table-cell stats-table-cell--value';
-    const label = document.createElement('span');
-    label.className = 'stats-swatch-label';
-    label.textContent = C;
-    cell.appendChild(label);
-    headerRow.appendChild(cell);
-  }
+  const crestHeaderLabels = [];
+  for (let i = 0; i < colorCount; i++) {
+    const cell = document.createElement('span');
+    cell.className = 'stats-table-cell stats-table-cell--value';
+    const label = document.createElement('span');
+    label.className = 'stats-swatch-label';
+    label.textContent = colorLabelForIndex(i);
+    cell.appendChild(label);
+    headerRow.appendChild(cell);
+    crestHeaderLabels.push(label);
+  }
@@
-  fields.crest = {
-    rows: crestRows,
-    summary: { colors: summaryColorCells, total: summaryTotal },
-    colorCount,
-  };
+  fields.crest = {
+    rows: crestRows,
+    summary: { colors: summaryColorCells, total: summaryTotal },
+    colorCount,
+    headerLabels: crestHeaderLabels,
+  };
*** End Patch

